# Database
DATABASE_URL="postgresql://pyknowledge:pyknowledge@localhost:5432/pyknowledge?schema=public"

# API Server
PORT=3000
NODE_ENV=development
JWT_SECRET="change-this-in-production-use-long-random-string"
JWT_EXPIRES_IN="7d"

# CORS (client origin)
CORS_ORIGIN="http://localhost:8080"

# Default institution for new registrations
DEFAULT_INSTITUTION_NAME="TRAC"
